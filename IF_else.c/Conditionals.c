#include <stdio.h>

int main(){
    int age = 5;
    if(age>5)
    {
        printf("We are inside if\n");
        printf("your age is greater then 10");
    }
    if(age%5==0)
    {
        printf("we are inside if\n");
        printf("your age is greater then 10\n");
    }
    return 0;
}